function [V, W]  = csp(left, right, row, column, left_trial, right_trial)
left_cov=[];
right_cov=[];
for i= 1:1:left_trial;
    trial=left(:,:,i);
    cov=trial-(ones([row,1])*ones([1,row])*trial*1/row);
    left_cov = left_cov + cov * cov';
end
left_cov=left_cov/left_trial;
    
for i= 1:1:l_trial;
    trial=left(:,:,i);
    cov=trial-(ones([row,1])*ones([1,row])*trial*1/row);
    left_cov = left_cov + cov * cov';
end
left_cov=left_cov/left_trial;   



