for i=1:1:25;
     temp=s(:,[i]);
     [output,n ]=ourfilter(temp,2,3,40,250);
     if i==1
         csvwrite('filtered.csv',output);
     
     else
         data = csvread('filtered.csv');
         data=[data output];
         csvwrite('filtered.csv',data);
     end
end